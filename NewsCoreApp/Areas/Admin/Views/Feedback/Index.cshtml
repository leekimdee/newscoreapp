@{
    ViewData["Title"] = "Index";
}
@section Scripts{
    <script src="~/app/controllers/feedback/index.js"></script>
    <script>
        var feedback = new feedbackController();
        feedback.initialize();
    </script>
}
<!-- Content Header (Page header) -->
<section class="content-header">
    <h1>
        Phản hồi từ khách hàng
        <small>Danh sách phản hồi</small>
    </h1>
    <ol class="breadcrumb">
        <li><a href="#"><i class="fa fa-dashboard"></i> Trang chủ</a></li>
        <li class="active">Danh sách phản hồi</li>
    </ol>
</section>

<!-- Main content -->
<section class="content">
    <div class="row">
        <div class="col-md-12">
            <div class="box">
                <div class="box-header with-border">
                    <h3 class="box-title">Tất cả phản hồi</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">
                    <table class="table table-bordered" id="tbl-list">
                        <thead>
                            <tr>
                                <th style="width: 100px">#</th>
                                <th>Tiêu đề</th>
                                <th>Họ tên</th>
                                <th>Điện thoại</th>
                                <th>Email</th>
                                <th>Ngày tạo</th>
                                <th style="width: 40px">Trạng thái</th>
                            </tr>
                        </thead>
                        <tbody id="tbl-content"></tbody>
                    </table>
                </div>
                <!-- /.box-body -->
                <!--Pagination-->
                <div id="pagingSection" class="box-footer">

                </div>
            </div>
        </div>
    </div>
</section>
<!-- /.content -->
<script id="table-template" type="x-tmpl-mustache">
    <tr>
        <th scope="row">
            <button class="btn btn-default btn-sm btn-detail" data-id="{{Id}}"><i class="fa fa-info"></i></button>
            <button class="btn btn-danger btn-sm btn-delete" data-id="{{Id}}"><i class="fa fa-trash"></i></button>
        </th>
        <td>{{Title}}</td>
        <td>{{Name}}</td>
        <td>{{Phone}}</td>
        <td>{{Email}}</td>
        <td>{{CreatedDate}}</td>
        <td>{{{Status}}}</td>
    </tr>
</script>
@Html.Partial("_DetailModal.cshtml")